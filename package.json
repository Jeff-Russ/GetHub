{
  "name": "gethub",
  "version": "0.0.1",
  "description": "Flexible and easy web-based tool for querying GitHub REST API (GET) endpoints",
  "main": "app.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "start": "node .",
    "dev": "nodemon . & npm run scss",
    "scss": "sass --watch src/:public/compiled-css/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "nodemon": "^2.0.20",
    "octokit": "^2.0.7",
    "sass": "^1.55.0"
  }
}
